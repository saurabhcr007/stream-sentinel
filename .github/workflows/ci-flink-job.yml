name: CI - Flink Job

on:
  push:
    branches: [ "main", "develop" ]
    paths:
      - 'flink-job/**'
      - 'core-engine/**'
      - 'pom.xml'
  pull_request:
    branches: [ "main", "develop", "release/*", "hotfix/*" ]
    paths:
      - 'flink-job/**'
      - 'core-engine/**'
      - 'pom.xml'

jobs:
  test-flink-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build and test Flink Job
        run: mvn clean test -pl flink-job -am -B
